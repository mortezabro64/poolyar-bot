name: PoolYar Hourly Price

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  price:
    runs-on: ubuntu-latest

    steps:
      - name: Run PoolYar Bot
        run: echo "PoolYar bot is ready"
